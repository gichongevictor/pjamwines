<!DOCTYPE html>
<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>sold items</title>
    <style>
        table, th, td {
             border: 1px solid black;
        }
    </style>
</head>
<body>

    <table>
        <tr>
            <th>Brand</th>
            <th>Price</th>
            <th>Pieces</th>
            <th>Totals</th>
            <th>Profit</th>
        </tr>
        <% for(var i=0; i < data.length; i++) {%>
        <tr>
            <td><%= data[i].product.brand%></td>
            <td><%= data[i].product.price%></td>
            <td><%= data[i].product.piece%></td>
            <td><%= data[i].product.piece*data[i].product.price%></td>
            <td><%= data[i].product.profit*data[i].product.piece%></td>
        </tr>
        <% } %>
        <tr>
            <td style="text-align: center;">Total Sales</td>
            <td style="text-align: center;" colspan="4"><%=sales_totals%></td>
        </tr>
        <tr>
            <td style="text-align: center;">Profits </td>
            <td style="text-align: center;" colspan="4"><%=job_profit%></td>
        </tr>
    </table>
</body>
</html>